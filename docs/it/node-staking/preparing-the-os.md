# Preparazione del Sistema Operativo

[TBD] Questa sezione dovrebbe descrivere come eseguire operazioni come la configurazione di un secondo disco rigido se applicabile, eseguire un test IOPS con `fio`, configurare lo spazio di swap... tutto ciò di cui qualcuno potrebbe aver bisogno. Il test `fio` è importante, ma le altre operazioni potrebbero essere opzionali.
